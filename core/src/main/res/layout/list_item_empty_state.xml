<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <import type="com.senykvolodymyr.core.recyclerview.listitem.EmptyStateListItem" />

        <variable
            name="model"
            type="EmptyStateListItem" />

    </data>

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/output_empty_state"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:elevation="?attr/elevationMedium"
        android:gravity="center"
        android:padding="?attr/paddingBig"
        android:text="@{model.message}"
        android:textColor="?attr/colorContentSecondary"
        android:textSize="?attr/textSizeBig"
        android:textStyle="italic"
        android:visibility="@{model.message != null ? View.VISIBLE : View.GONE}"
        tools:text="Empty" />

</layout>
